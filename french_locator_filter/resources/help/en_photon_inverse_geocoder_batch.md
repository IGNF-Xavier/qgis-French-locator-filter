- Description:

Reverse geocoding using Photon from a vector layer.

- Parameters:

| Input                  | Parameter | Description                                              |
|-------------------------|-----------|----------------------------------------------------------|
| Input vector layer      | `INPUT`   | Input vector layer (point or polygon type).                         |

- Outputs:

| Output                  | Parameter | Description                                  |
|--------------------------|-----------|----------------------------------------------|
| Output vector layer      | `OUTPUT`  | Vector layer with reverse geocoding. |

Processing name: `french_locator_filter:photon_inverse_geocoder_batch`
